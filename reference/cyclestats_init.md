# Prepare data for use within the app

Reads the Strava activities CSV, filters to cycling rides, converts
units, and creates the global objects used by the UI and server.

## Usage

``` r
cyclestats_init()
```
