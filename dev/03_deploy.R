## Development script — deployment

# Deploy to shinyapps.io
# rsconnect::deployApp()

# Snapshot renv after adding packages
# renv::snapshot()

# Build package tarball
# devtools::build()

# Install locally
# devtools::install()
